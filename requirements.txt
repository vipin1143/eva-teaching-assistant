# ══════════════════════════════════════════════════════════════
# EVA - Emotion-Adaptive Virtual Assistant
# Production Requirements
# ══════════════════════════════════════════════════════════════

# ── Core Backend ──────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
websockets==12.0
pydantic==2.7.0
python-dotenv==1.0.1

# ── AI / ML (Emotion Detection) ───────────────────────────────
onnxruntime==1.17.1          # ONNX model inference (EVA + FER+)
opencv-python-headless==4.9.0.80  # Face detection & preprocessing
numpy==1.26.4                # Array operations
mediapipe==0.10.14           # Head pose estimation
ultralytics>=8.0.0           # YOLOv8 face detection

# ── Text Sentiment Analysis ───────────────────────────────────
vaderSentiment==3.3.2        # Chat sentiment

# ── Database ──────────────────────────────────────────────────
sqlalchemy==2.0.29           # ORM
aiosqlite==0.20.0            # Async SQLite driver

# ── Screen Capture (Zoom/Meet Integration) ────────────────────
mss==9.0.1                   # Screen capture
pyautogui==0.9.54            # Page cycling
pytesseract==0.3.10          # OCR for student names
Pillow==10.3.0               # Image processing

# ── AI Suggestions (Optional - choose one) ────────────────────
groq==0.9.0                  # Free tier - Recommended
# openai==1.23.0             # For DeepSeek/OpenRouter
# google-generativeai==0.5.2 # For Gemini

# ── Utilities ─────────────────────────────────────────────────
httpx==0.27.0                # HTTP client
watchfiles==0.21.0           # File watching

# ── Optional: Better Face Alignment ───────────────────────────
# dlib                       # Uncomment if you want face alignment
                             # Requires: cmake, Visual Studio (Windows)
                             # Install: pip install dlib

# ── Optional: Visualization ───────────────────────────────────
# matplotlib==3.8.4          # For confusion matrix (evaluation only)

# ── Production Extras ─────────────────────────────────────────
# gunicorn==21.2.0           # Production ASGI server
# redis==5.0.0               # For session caching (large deployments)
# psycopg2-binary==2.9.9     # PostgreSQL driver (if not using SQLite)
